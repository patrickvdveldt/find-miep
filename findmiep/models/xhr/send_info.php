<?php


/*
  require "../findmiep/controllers/db_connector.php";

  $pdo = pdo();

  if (isset($_POST['submit'])) {

    $naam = $_POST['naam'];
    $email = $_POST['email'];
    $bedrijf = $_POST['bedrijf'];
    $curtime = $_POST['curtime'];

    $insertmiep= $pdo->prepare('INSERT INTO mieps(naam, email, bedrijf, curtime) VALUES(?,?,?,?)');
    $insertmiep->execute(array($naam,$email,$bedrijf,$curtime));

    echo "toegevoegd";

  }*/

  
 ?>
